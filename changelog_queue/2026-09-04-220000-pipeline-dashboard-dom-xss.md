- **Последний (пятый) алерт CodeQL закрыт — дашборд конвейера видео переведён с `innerHTML` на DOM-сборку (хвост [H4025 (Opus 5) — BookIndex: allow-list URL sanitisers and the boot-catch XSS sink](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H4025-Opus_BookIndex_url-sanitiser-and-boot-xss_03.09.26.md), OxAlpha `z-ai/glm-5.3-flash`).** `js/xss-through-dom` в [pipeline/index.html](https://github.com/gasyoun/BookIndex/blob/main/pipeline/index.html) (alert #24): таблица собиралась шаблонной строкой в `innerHTML`, а ссылки шли как `href="${...}"` с экранированием кавычек, но **без проверки схемы** — `javascript:` проходил бы. Шаблон в [scripts/build_pipeline_dashboard.py](https://github.com/gasyoun/BookIndex/blob/main/scripts/build_pipeline_dashboard.py) переписан по правилу [FINDINGS.md](https://github.com/gasyoun/BookIndex/blob/main/FINDINGS.md) §6: строки таблицы строятся через `createElement`/`textContent`, `href` — только через allow-list `http:`/`https:` (`safeUrl` через `new URL()`). Открытых алертов CodeQL — **0**. Побочный улов: у этой страницы не было своего regenerate-and-diff гейта, и артефакт протух — данные ушли вперёд (177 видео, `96ddecf54`), а страница осталась на 176; пересобрано из текущих данных (стоит завести гейт). Парити-проверка Playwright headless (старый и новый билд бок о бок): те же 14 stale-флагов, те же ссылки и `rel`, те же pills и цвета стадий, фильтры `notcat`/поиск работают, маркеры `∉`/`~`/`⚠` на месте, 0 ошибок страницы; отличается только межячеечный whitespace текстовых узлов, который таблица не рендерит.
