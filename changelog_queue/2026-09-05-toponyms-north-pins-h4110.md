- **Toponym map: north-Russia label arrangement PINNED (H4110, MG 05-09-2026, fourth ask: «Кольский переехал левее… поставь рядом с ним Архангельскую область, справа. А под Кольским — Финляндия. Why does this error keeps returning?»).** Root cause of the recurring error was systemic: no MG-approved placement survived a variant rebuild — only the handful of `B8_LABEL_BIAS`/`B10_LABEL_BIAS` rows were pinned, everything else re-derived from scratch each rev, and the «Архангельская справа от Кольского» wish had been closed as «measured impossible» (rev-12 note) instead of implemented with a fitting mechanism, so it re-opened on every render; Кольский's own label was born 17 mm left of its B8-approved slot because it never had a bias row. Fix: new `NORTH_LABEL_PINS` — Кольский returns right of the peninsula (the B8-approved slot, label center (373, 345) units), Архангельская область sits next to it to the RIGHT as a two-line stack «Архангельская / область» (the stack halves the 37.5 mm name to 23 mm and fits the frame where the one-liner never could), Финляндия sits below Кольский (328.7, 369.4). Pins are RESERVED before the label walk (fail-loud `LABEL_PIN` errors on frame/clash — never a silent fallback), other labels route around them, and `noLeader` skips the вынос ties (the straight 61 mm Архангельская leader was the class MG rejects, and the Кольский/Финляндия tie lines measured 0.15–0.49 u from neighbour chip centers). Stale rev-12 «MEASURED IMPOSSIBLE» note replaced; the superseded `[144,12]` bias row deleted in the B9/B10 clones. Final-render verified on both sheets: label coordinates exact to 0.1 u, «см. врезку» = 0, `#444444` fills intact, `label_chip_violations` 0; frozen sheets (A/C/D3/b2..b8) byte-identical; `npm run print:toponyms:check` green post-commit; live URLs 200. (OxAlpha, `z-ai/glm-5.3-flash`)
