- **Стянут двухдневный дрейф сборочного вывода: `aaz-index.html` + 699 пререндеренных страниц пересобраны, CI-гейт расширен на весь вывод сборки (попутно найдено при закрытии хвоста [H4025](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H4025-Opus_BookIndex_url-sanitiser-and-boot-xss_03.09.26.md), OxAlpha `z-ai/glm-5.3-flash`).** CI на `main` был красный с предыдущего дня: гейт `Ensure committed aaz-index.html is in sync` падал — `npm run build` (пересборка [aaz-index.html](https://github.com/BookIndex/blob/main/aaz-index.html)) менял CSP-хэши и `build_id`, потому что коммит не пересобирался после изменений рантайма (H4023 bootlog-флаг, H4025 санитайзеры) и данных (H4051-B10 `scale_rank`, модуль toponyms 202092 → 204976 байт). Главное: гейт смотрел **только на точку входа**, а `npm run build` перезаписывает ещё **699 пререндеренных страниц** в `all/ ethnonyms/ languages/ lexicon/ lexicon_reverse/ lexicon_tech/ materials/ names/ scholar/ subject/ toponyms/` — без гейта они протухли молча: в каждую страницу **инлайнится весь рантайм** (глубокие ссылки раздавали бандл старше H4023/H4025, т.е. без bootlog-флага и с ослабленным санитайзером) и старые CSP-хэши/манифесты. Пересобрано `npm run build` целиком (700 файлов, у каждого ровно 8 строк: CSP, `build_id`, манифест, инлайн-ронтайм); гейт в [ci.yml](https://github.com/BookIndex/blob/main/.github/workflows/ci.yml) теперь диффует весь вывод сборки — и в синк-проверке, и в `Vite build smoke`. Урок повторяет pipeline-дашборд из той же смены: артефакт без своего regenerate-and-diff гейта протухает молча.
