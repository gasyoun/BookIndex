- **Toponym print pipeline wired into CI; ru collation pinned (H4051).** The generator behind eleven revisions of MG feedback was in no test and no workflow - "frozen sheets byte-identical" was enforced by a human reading `git status`. New `npm run print:toponyms:check` (regenerate + `git diff --exit-code -- print/*.svg`, with the generator's hardFail gates inside) runs as a CI step matching the repo's five existing regenerate-and-diff gates; both print scripts joined `check:js`. Group order - which decides the 1..83 numbering and therefore every byte of every sheet - is owned by ICU-version-dependent `localeCompare(…, "ru")`; a drifted order now fails loudly against `RU_COLLATION_SHA = 42b3a8abe09d1ac8` (Node v24.15.0 / full ICU, 04-09-2026, 83 groups) instead of silently renumbering the book on another box. (OxAlpha, `z-ai/glm-5.3-flash`)
