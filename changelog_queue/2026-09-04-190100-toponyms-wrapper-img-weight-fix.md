- **Toponym wrapper pages reference sheets instead of inlining them - print/ 237.9 -> 80.6 MB, versions page 63.8 MB -> 9.2 KB (H4051 weight fix, MG ruling 04-09-2026).** 99.1% of a map SVG is the Natural Earth land path (5.5 of 5.57 MB on b8), and every variant stored it four times: the `.svg`, `*-print.html`, `*.html`, and again inside `toponyms-map-versions.html` - a 63,755,814-byte tracked blob past GitHub's 50 MB warning, on an ~836 MB Pages site against the 1 GB limit, ~5 revisions from a refused push. Wrappers now use `<img src>` (vector preserved in print, every URL and every `.svg` byte unchanged); the versions page fetches sheets lazily and extracts D1 once from tag `v4.17.7` into frozen `d1-*.svg` so no variant depends on `git show` at page-build time. Cost, documented: the HTML is no longer a detached single file - MG reviews via gasyoun.github.io URLs, so nothing is lost. (OxAlpha, `z-ai/glm-5.3-flash`)
