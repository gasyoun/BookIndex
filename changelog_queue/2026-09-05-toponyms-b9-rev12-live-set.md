- **Toponym map: rev-12 wishes ported to the LIVE B9 sheet, not just B10 (H4109, MG 04-09-2026: «Последние замечания внедрены не полностью»).** B9map (the accepted working variant) now carries all three rev-12 changes B10 already had: (1) `softFill: true` — discussed markers fill dark-gray `#444444` instead of `#111111`; (2) «см. врезку» caption stays absent (B9 already drew via `insetSourceArrow`, not `refRect` — verified 0 occurrences on final render); (3) the three moved labels — Германия · ГДР and Шри-Ланка · Цейлон land at essentially the same neighbourhoods as B10 (~22,79.5 and ~126.9,136.6 mm); Швеция's B9-specific bias was re-derived (not copied from B10) because B9's denser obstacle field — no `scaleRankedNames`/`tightUpwardBoxes` — pushed the copied B10 offset into a gate-failing 31.5 mm leader; the re-derived offset clears it at 29.7 mm. Frozen sheets (A/C/D3/b2..b8, B10) stay byte-identical; only `toponyms-map-b9-map.svg` + the report/versions page changed. (OxAlpha, `z-ai/glm-5.3-flash`)
