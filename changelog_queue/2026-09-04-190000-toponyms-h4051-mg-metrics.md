- **Toponym map pipeline measures what MG judges (H4051 audit, MG rev 11 ruling 04-09-2026).** Four variants in a row shipped green gates and were rejected because the gates measured the placer's own bookkeeping, not MG's criteria. Every sheet now reports `links_drawn` (by kind, all 7 line sites centralised), `max_link_mm`, `names_requested/drawn/not_drawn`, `names_at_true_place`, `label_offset_p50/p90/max_mm` and `chip_ink_overlap_*`; the numbers land in `toponyms-map-report.json` (`mg_metrics`) and as green rows on [toponyms-map-versions.html](https://gasyoun.github.io/BookIndex/print/toponyms-map-versions.html). Baselines now on record: B8 draws 94 lines, silently drops 25 of 63 names, keeps only 13 within the 10 mm budget, median label 13.2 mm from its dot. Escapes / label-chip gates now measure the rectangle the placer actually reserved (the old re-derived box was wrong for upward multi-line stacks); a sheet emitting `NaN`/`Infinity` refuses to write; legend rows can follow the oldest-attested-first ruling via `NAME_ORDER_OLDEST_FIRST` opt-in with `display_order_conflicts: 2` tracked (frozen legends byte-identical). Audit record: [docs/TOPONYM_MAP_H4051_AUDIT_AND_METRICS_2026-09-04.md](https://github.com/gasyoun/BookIndex/blob/main/docs/TOPONYM_MAP_H4051_AUDIT_AND_METRICS_2026-09-04.md). All frozen `.svg` byte-identical. (OxAlpha, `z-ai/glm-5.3-flash`)
