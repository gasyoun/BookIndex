- **H5106 — DOM XSS source-to-sink practice drill shipped.** 12-case mutation corpus in [tests/security/dom_xss_drill/](https://github.com/gasyoun/BookIndex/blob/main/tests/security/dom_xss_drill/) distilled from merged security PRs #141 (data-bearing innerHTML), #294 (URL allow-list gaps + bootstrap catch sink) and #299 (dashboard DOM assembly): 8 exploitable shapes (P1/P2) + 4 near-miss controls (2 SAFE, 2 context-sensitivity P3). New capability-aware taint checker `check_taint.py --improved` detects 8/8 exploitable sinks with source→transform→sink traces and labels 0 safe near-misses P1/P2 — GOAL PASS on 4 consecutive runs; the grep-style naive baseline misses B1/B2 and false-labels A3 as P1 (non-vacuous canary, captured in RUNS_H5106.log). Practice artifacts only — no production code changed. (OxAlpha, `zai-coding-plan/glm-5.3-flash`)
