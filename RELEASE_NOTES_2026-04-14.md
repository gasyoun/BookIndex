# Зализнякиада — релизный срез (2026-04-14)

## Статус
- Все backlog issues `#1-#15` в `gasyoun/BookIndex` закрыты.
- Финальная сборка обновлена: `aaz-index.html`.

## Что вошло в релиз
- Дедупликация рендеров и таблица диспетчеризации вкладок.
- Hash-навигация, история, кнопка назад, глобальный поиск, копирование ссылки карточки.
- Мобильный bottom-sheet карточки и горячие клавиши (`/`, `Esc`, `←`, `→`).
- Сохранение состояния интерфейса в `localStorage` с версией схемы и soft-reset.
- Глоссарий ↔ Лексика (двусторонние переходы), хлебные крошки в шапке.
- Темная тема (CSS variables + persistence).
- Virtual scroll для больших списков.
- Кэш тяжелых агрегатов (histogram/heatmap/graph) + debug-метрики времени рендера.

## Проверка перед фиксацией
- `node --check v3_app.js` — OK.
- `python runtime_test.py` — `20/20` (OK).
- `gh issue list --state open` — пусто (открытых задач нет).
