# Отчет по перелинковке (2026-04-19)

## Контекст проверки
- Основание: `C:\Users\user\Downloads\codex_instruction.md` (TASK-1 ... TASK-5).
- Проверенный код: `v3_app.js`, `v3_template.html`, собранный `aaz-index.html`.

## Исправление по замечанию
- Проблема: словоформы `английским` / `французским` не линковались в контекстах.
- Причина: в генераторе словоформ для прилагательных на `-ий/-ой` не хватало инструментального варианта `-им`.
- Исправление: в `buildContextLinkMatchTerms()` добавлена форма `stem + "им"`.

## Результаты аудита TASK-1 (автолинки в contexts)
- Секции проверки: `names`, `toponyms`, `ethnonyms`, `languages`.
- Контекстных фрагментов проверено: `1761`.
- Фрагментов, где автолинк создает хотя бы одну ссылку: `1317` (`74.79%`).
- Всего сгенерированных автоссылок: `2783`.
- Распределение ссылок по типам:
  - `languages`: `2424`
  - `names`: `15`
  - `ethnonyms`: `195`
  - `toponyms`: `149`
- Контрольные словоформы после фикса:
  - `английским`: `17` попаданий
  - `французским`: `21` попадание
  - `латыни`: `65` попаданий
- Эвристическая проверка класса `-ским`:
  - непрокрытых кейсов не найдено.

## Результаты аудита TASK-2 (subject_index → crosslinks)
- Элементов `subject_index`: `88`.
- С crosslinks: `88/88`.
- Без crosslinks: `0`.
- Примечание: в текущей реализации ссылки строятся через fallback по page-overlap (это шире, чем только exact-match).

## Результаты аудита TASK-3 (двунаправленные связи персоналий)
- Всего ребер `edges`: `60`.
- Персоналий в `names`: `96`.
- Персоналий с хотя бы одной связью: `41`.
- Персоналий, у которых есть только обратные связи (reverse-only): `12`.
- Вывод: reverse-ветка действительно нужна, иначе часть связей в карточках не отображается.

## Результаты аудита TASK-4 / TASK-5
- `breadcrumb-nav` в шаблоне: есть.
- `renderBreadcrumb()` и `buildBreadcrumbTrail()` в `v3_app.js`: есть.
- `kwic-jump-btn` + `window._pendingKwicTerm`: есть.
- Блоки `subject-crosslinks` / `crosslink-badge`: есть.
- Блок `relation-chip` + `getReverseEdgesIndex`: есть.

## Что осталось неидеальным (содержательно)
- В рамках TASK-1 автолинк по-прежнему лучше покрывает языковые/терминологические упоминания, чем персоналии (по данным выборки это нормально, но это видимый перекос).
- Fallback-перелинковка в `subject_index` может давать тематически близкую, но не всегда «буквально точную» связку (из-за выбора по overlap страниц).

## Технические ограничения текущей среды
- `python runtime_test.py` не прошел в этой среде из-за отсутствия `node` в PATH:
  - `[env] FAIL: Node.js binary not found.`
